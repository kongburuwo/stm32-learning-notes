# 初识单片机
- 概念：单片机（Single-Chip Microcomputer）单片机微型计算机，是一种集成电路芯片，把具有数据处理能力的
中央处理器 CPU、随机存储器 RAM、闪存 flash、多种I/O口和中断系统、定时器/计数器等功能（可能还包
括显示驱动电路、脉宽调制电路、模拟多路转换器、A/D转换器等电路）集成到一块硅片上构成的一个小而
完善的微型计算机系统

# 时钟树
## 时钟概念
- 时钟是由电路产生的具有周期性的脉冲信号，相当于单片机的心脏，给单片机提供一个统一的信号。
- 要想使用单片机的外设必须开启相应的时钟：
    1. 高速外部时钟（HSE）
    2. 高速内部时钟（HSI）
    3. 倍频器/锁相环（PLL）
    4. 低速内部时钟（LSI）
    5. 低速外部时钟（LSE）
    6. 系统时钟（SYSCLK）
    7. AHB高速总线（HCLK）
        1. APB1时钟总线/最大36M（PCLK1）
            1. 外设
        2. APB2时钟总线/最大72M（PCLK2）
            1. 外设
        3. 内核
        4. 外设
## 时钟树设置顺序
1. 配置时钟源、时钟分频器
    - HAL_RCC_OscConfig();
2. 系统时钟、AHB总线、APB1总线、APB2总线
    - HAL_RCC_ClockConfig();
3. 使能外设时钟(例如GPIOA时钟)
    __HAL_RCC_GPIOA_CLK_ENABLE();
4. 拓展外设时钟（RTC/ADC/USB）
    - HAL_RCCEx_PeriphCLKConfig();

*相关c文件*
```c
#include "sys.h"

//开启HSE → 配置PLL → 配置SYSCLK/总线分频 → 配置Flash等待周期
void stm32_clock_init(uint32_t pllmul)
{
	
	
	HAL_StatusTypeDef ret = HAL_ERROR;
	
	RCC_OscInitTypeDef rcc_osc_init = {0};
	RCC_ClkInitTypeDef rcc_clk_init = {0};
	
	
	//配置振荡器（HSE + PLL）
	rcc_osc_init.OscillatorType = RCC_OSCILLATORTYPE_HSE;
	rcc_osc_init.HSEState = RCC_HSE_ON;
	rcc_osc_init.HSEPredivValue = RCC_HSE_PREDIV_DIV1;
	rcc_osc_init.PLL.PLLState = RCC_PLL_ON;
	rcc_osc_init.PLL.PLLSource = RCC_PLLSOURCE_HSE;
	rcc_osc_init.PLL.PLLMUL = pllmul;
	
	ret = HAL_RCC_OscConfig(&rcc_osc_init);
	if(ret != HAL_OK)
	{
		while(1);
	}
	
	//配置系统时钟树（SYSCLK、HCLK、PCLK1/2）
	rcc_clk_init.ClockType = RCC_CLOCKTYPE_SYSCLK | RCC_CLOCKTYPE_HCLK | RCC_CLOCKTYPE_PCLK1 | RCC_CLOCKTYPE_PCLK2;
	rcc_clk_init.SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK;
	rcc_clk_init.AHBCLKDivider = RCC_SYSCLK_DIV1;
	rcc_clk_init.APB1CLKDivider = RCC_HCLK_DIV2;
	rcc_clk_init.APB2CLKDivider = RCC_HCLK_DIV1;
	
	ret = HAL_RCC_ClockConfig(&rcc_clk_init,FLASH_LATENCY_2);
	
		if(ret != HAL_OK)
	{
		while(1);
	}
	
	
}
```
