#include <stdio.h>
#include <limits.h>

int main()
{
    //short a = -1;
    
    //unsigned int无符号整型占位符用的%u

    printf("short MAX %d,short MIN %d\n", SHRT_MAX, SHRT_MIN);

    printf("long size is %d\n",sizeof(long long));

    return 0;
}