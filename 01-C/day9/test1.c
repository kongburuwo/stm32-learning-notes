#include <stdio.h>

// 编写一个函数，计算两个整数的最大公约数。
/** 欧几里得算法（辗转相除法）
欧几里得算法是计算两个整数最大公约数的经典方法，基于以下原理：
如果 a 和 b 是两个正整数，且 a > b，那么 gcd(a, b) = gcd(b, a % b) */ 

int main()
{
    int a, b;
    printf("请输入两个正整数：");
    scanf("%d %d", &a, &b);

    

    

    return 0;
}